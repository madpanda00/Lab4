<div class="container">
    <div class="card mb-3 color-ef">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            {{ form_start(form) }}
            <div class="form-group">
                <div class="mt-3">{{ form_widget(form.content) }}</div>
            </div>
            <button type="submit" class="btn btn-sm align-middle btn-outline-secondary"
                style="margin-bottom: 8px;">Добавить комментарий</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>